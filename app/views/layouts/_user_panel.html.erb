<div id="user_panel">
  <% if @current_user %>
    <%= link_to @current_user.name, users_url %> / <%= link_to 'Logout', logout_url, method: :delete %>
  <% else %>
    <%= link_to 'Login', login_url, remote: true %> / <%= link_to 'Register', new_user_url %>
  <% end %>
</div>